# Runtime dependencies.
#
# Core packages are pinned to the exact versions already present in the project
# virtualenv, so deploys are reproducible and a breaking upstream release can
# never take the site down without warning.

Flask==3.1.3
Flask-SQLAlchemy==3.1.1
SQLAlchemy==2.0.49
Werkzeug==3.1.8
psycopg2-binary==2.9.11
python-dotenv==1.2.2
twilio==9.10.5

# Added by the 3.0 upgrade.
# Ranges (not exact pins) because these were not previously installed; run
#   pip install -r requirements.txt && pip freeze > requirements.lock.txt
# once locally to capture exact resolved versions.
Flask-WTF>=1.2,<2.0
Flask-Migrate>=4.0,<5.0
cloudinary>=1.40,<2.0
requests>=2.31.0,<3.0
